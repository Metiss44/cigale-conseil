/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cigale.matisscottard.com', 'cdn.matisscottard.com'],
  },
}

module.exports = nextConfig
