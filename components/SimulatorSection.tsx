// SimulatorSection removed temporarily per request.
// Keeping an empty stub to avoid import errors elsewhere while the simulator
// feature is out of scope. Re-add implementation when needed.

export const SimulatorSection: React.FC = () => {
  return null;
};
